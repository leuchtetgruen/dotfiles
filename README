After initializing & updating all the submodules (dont forget the submodules in the vim-folder) create symlinks:

mkdir -p ~/.config/nvim
ln -s ~/dotfiles/vim/init.vim ~/.config/nvim/init.vim
ln -s ~/dotfiles/zshrc ~/.zshrc

